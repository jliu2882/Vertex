Implement error handling and security measures [probably vscode to scan project]
implement refresh tokens vs just one token for authentication (a frequently used that expires and needs refreshing, and "big" token for entire session)
compare project with other implementation

implement a barebone skeleton of react (and get rough understanding of why things are the way they are again)
connect the frontend to backend(have something that implements all 6 routes)
polish backend/frontend individually
implement unit tests for backend

add observability/logging (not as important as just havving a deployed app but)

resume discussion on frontends tuff but dont mention tanSTack intereste d if repeated

clean up dockerignores and other files like that